public class EllysFractions {
	long[][] choose;
	
	public long getCount(int N) {
		long count = 0;
		int primes = 0;
		nChooseKDP(N, N);
		for (int i = 2; i <= N; i++) {
			boolean prime = true;
			for (int j = 2; j*j <= i; j++)
				if (i % j == 0) {
					prime = false;
					break;
				}
			
			if (prime)
				primes++;
			for (int k = 1; k < primes; k++)
				count += choose[N][k];
		}
		return count / 2;
	}
	
	void nChooseKDP(int n, int k) {
		choose = new long[n + 1][k + 1];
		int i, j;
		for (i = 0; i <= n; i++)
			choose[i][0] = choose[i][i] = 1;
		for (i = 1; i <= n; i++)
			for (j = 1; j < i; j++)
				choose[i][j] = choose[i - 1][j - 1] + choose[i - 1][j];
	}
	
	public static void main(String[] args) {
		EllysFractions f = new EllysFractions();
		System.out.println(f.getCount(3));
	}
}
